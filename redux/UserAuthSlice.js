import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";


const initialState = {
    usersList:[],
    loading:false,
    user:{}
}
 export const userListIteam= async()=>{
   await URL.apply((data)=>{
    return data
   })
}